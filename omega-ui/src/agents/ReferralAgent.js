// File: src/agents/ReferralAgent.js

import { logAgentActivity } from '../utils/logger';

const ReferralAgent = {
  name: 'ReferralAgent',
  description: 'Manages referral campaigns, logic, bonuses, tracking and fraud prevention.',

  async execute(payload) {
    logAgentActivity('ReferralAgent', 'Processing Referral Logic', payload);

    const { referrerId, referralId, isValid } = payload ​:contentReference[oaicite:0]{index=0}​
